Use ${GODOGEN_COMMAND} to generate or update this Babylon.js browser game from a natural language description.

This repo is a Babylon.js + TypeScript + Vite project. Keep `npm run dev` running during active work when possible so the game stays available at `http://127.0.0.1:5173` for user review and browser capture.
